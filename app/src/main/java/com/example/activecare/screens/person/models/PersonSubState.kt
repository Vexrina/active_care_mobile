package com.example.activecare.screens.person.models

enum class PersonSubState {
    Default, Stat, Workouts,
    Recommendations, Devices,
    Settings, ProfileSetting,
    NotificationSettings,
    DeviceSettings,
}